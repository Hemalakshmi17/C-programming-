#include <stdio.h>

// function definition
int add(int a, int b) {
    return a + b;  // return sum
}

int main() {
    int result = add(10, 20);  // function call with arguments
    printf("Sum = %d\n", result);
    return 0;
}
